version: 2

# Try using these source definitions instead!
sources:
  - name: raw_data
    schema: "{{ target.schema }}"
    description: Raw Data for Climbing Weather
    tables:
      - name: raw_nws
        description: One record per location and updated hourly forecast per hour. 
      - name: raw_open_beta_countries
        description: One record per counry found in the Open Beta Graph QL. 
      - name: raw_open_beta_areas
        description: One record per area found in the Open Beta Graph QL. 